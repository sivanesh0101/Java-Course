#

## 5. Operators in Java

### Arithmetic Operators
```java
int a = 10, b = 5;
System.out.println(a + b); // 15
System.out.println(a - b); // 5
System.out.println(a * b); // 50
System.out.println(a / b); // 2
System.out.println(a % b); // 0
```

### Relational Operators
```java
System.out.println(a > b);  // true
System.out.println(a == b); // false
```

### Logical Operators
```java
System.out.println(a > b && b > 0); // true
System.out.println(a > b || b < 0); // true
System.out.println(!(a > b));       // false
```

### Assignment Operators
```java
a += 5; // a = a + 5
```

### Unary Operators
```java
int x = 5;
System.out.println(++x); // pre-increment
System.out.println(x++); // post-increment
```

### Ternary Operator
```java
int age = 18;
String result = (age >= 18) ? "Adult" : "Minor";
System.out.println(result);
```

### Bitwise Operators
```java
System.out.println(5 & 3); // AND
System.out.println(5 | 3); // OR
System.out.println(5 ^ 3); // XOR
```

---

## Extra Topics for Beginners
- Comments in code
- `System.out.printf` for formatted output
- Java naming conventions

---

## Practice Challenges
1. Create a program to calculate the area of a rectangle, circle, and triangle using variables and constants.
2. Write a program that takes a temperature in Celsius and converts it to Fahrenheit.
3. Create a student grade calculator using arithmetic and ternary operators.
4. Create a program that swaps two numbers without a third variable.
5. Create a currency converter (USD ↔ INR).
---
