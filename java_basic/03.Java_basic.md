

## 🚀 Key Feature: 
### Platform independed, WORA

> **WORA** stands for **Write Once, Run Anywhere**

✅ You can write Java code once and run it on **any operating system** that supports the Java Virtual Machine (JVM), without rewriting the code.

📌 Example:  
Write a program on Windows → Run the same code on Linux, macOS, or any device with a JVM installed.

---

### 🧰 JVM, JDK, JRE – What’s the Difference?

| Tool | Description |
|------|-------------|
| **JVM (Java Virtual Machine)** | Runs `.class` files (compiled Java bytecode) |
| **JRE (Java Runtime Environment)** | JVM + libraries to **run** Java programs |
| **JDK (Java Development Kit)** | JRE + tools like `javac` to **write and compile** Java programs |

![JDK](/images/JVM%20architecture.png)

📝 To **develop** Java apps, you need the **JDK**.  
To only **run** them, the **JRE** is enough.

---

## 👨‍💻 Writing Your First Java Program

### 📂 Step-by-Step

1. Open your code editor (like VS Code)
2. Create a new file: `HelloWorld.java`
3. Type the following code:

```java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

---

### 🔍 Code Explanation

| Line | Meaning |
|------|---------|
| `public class HelloWorld` | Declares a public class named `HelloWorld` |
| `public static void main(String[] args)` | Entry point of the Java program |
| `System.out.println(...)` | Prints text to the screen (console output) |

📢 `main` method is **mandatory**. It tells Java where to begin executing the code.

---

### ▶️ How to Compile and Run (using terminal)

1. **Compile:**
   ```bash
   javac HelloWorld.java
   ```

   🔄 This creates a file `HelloWorld.class` (Java bytecode)

2. **Run:**
   ```bash
   java HelloWorld
   ```

3. ✅ Output:
   ```
   Hello, World!
   ```

---

### 🎯 Practice Tasks

1. Modify the program to print your **name**.
2. Add one more line:  
   ```java
   System.out.println("Welcome to Java Programming!");
   ```
3. Try changing the class name (remember to rename the file too!).

---